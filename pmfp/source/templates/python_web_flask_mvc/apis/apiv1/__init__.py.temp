from flask_restplus import Api
from flask import Blueprint
from namespaces import ping
from .namespaces import echo

blueprint = Blueprint('apiv1', __name__, url_prefix='/api/v1')
api = Api(blueprint,
          title='Test Api',
          version='1.0',
          description='A description'
          )

api.add_namespace(echo, path='/test/echo')
api.add_namespace(ping, path='/test/ping')

