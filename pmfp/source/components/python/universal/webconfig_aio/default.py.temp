import os
from pathlib import Path


DEFAULT_SETTING = {
    "APP_NAME": "${project_name}",
    "LOG_LEVEL": "DEBUG",
    "UVLOOP": True,
    "PORT": 5000,
    "HOST": "0.0.0.0"
}
DEFAULT_PATH = Path("./config.json")