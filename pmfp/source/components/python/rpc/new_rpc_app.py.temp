from core import App
from view_clz import RPC
from logger import log

def init_app(config):
    log.initialize(
        app_name=config["APP_NAME"],
        log_level=config["LOG_LEVEL"]
    )
    app = App(config)
    app.register_instance(RPC())
    return app
