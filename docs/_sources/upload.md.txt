# upload

subcommand upload can manage the git push and upload your model to pypi or your own repertory.

if you want to upload your model to pypi you should use flag `-r` first.
then use `-p` to upload it to pypi.

if you want to upload your project to git.you can just use `-g` and write a commit message.
